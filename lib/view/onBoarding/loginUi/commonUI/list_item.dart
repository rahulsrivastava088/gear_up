class ListItem {
  final int index;
  bool isSelected;

  ListItem({required this.index, this.isSelected = false});
}

class SelectLevelListItem {
  final int index;
  bool isSelected;
  SelectLevelListItem({required this.index, this.isSelected = false});
}
